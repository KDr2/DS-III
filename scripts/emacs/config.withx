#!/bin/sh
#-*- mode: sh -*-
# toolkit: gtk/gtk2/gtk3/...
# xwidgets depends on libwebkit2gtk-4.0-dev

./configure --prefix=$PROG_DIR/emacs \
  --with-x --with-xft --with-xwidgets --with-x-toolkit=gtk3 \
  --with-modules \
  --without-pop --without-selinux
