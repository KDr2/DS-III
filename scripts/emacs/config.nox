#!/bin/sh
#-*- mode: sh -*-
./configure --prefix=${PREFIX-/data0/usr/local/emacs} \
            --with-modules \
            --without-x --with-x-toolkit=no \
            --without-pop --without-selinux

# --without-makeinfo \
