#!/bin/sh
#-*- mode: sh -*-
./configure --prefix=${PREFIX-$HOME/programs/emacs-nox} \
            --with-modules \
            --without-x --with-x-toolkit=no \
            --without-pop --without-selinux

# --without-makeinfo \
