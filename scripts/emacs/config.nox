#!/bin/sh
#-*- mode: sh -*-
./configure --prefix=${PREFIX-$PROG_DIR/emacs-nox} \
            --with-modules \
            --without-x --with-x-toolkit=no \
            --without-pop --without-selinux

# --without-makeinfo \
